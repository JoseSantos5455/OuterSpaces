const Username = (props) => {
  return (
    <input
      type="text"
      placeholder={props.name}
      className="py-[15px] px-[10px] border-[1px] border-gray-400 border-solid rounded-[5px] font-[15px] w-full"
    />
  );
};

export default Username;
0;
